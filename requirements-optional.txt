# 可选增强依赖：缺失时自动回退（Pillow / exifread），macOS 默认不安装
# pyvips 需要系统 libvips（macOS: brew install vips）；PyPI 上最高版本为 3.2.0
pyvips>=3.2.0
# pyexiftool 需要系统 Perl exiftool 可执行文件
pyexiftool>=0.5.0
customtkinter>=5.0.0
